<%= form_for(@medical_camp) do |f| %>
  <% if @medical_camp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@medical_camp.errors.count, "error") %> prohibited this medical_camp from being saved:</h2>

      <ul>
      <% @medical_camp.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
